on-notify=exec canberra-gtk-play -i message-new-instant

anchor=top-right
font=monospace 10
background-color=#37404fFF
text-color=#FFFFFFFF
width=350
## height=600
max-history=10
margin=10,10,10
padding=5
border-size=1
border-color=#8691a3FF
border-radius=4
default-timeout=10000
group-by=summary
icons=1

[grouped]
format=<b>%s</b>\n%b

# makoctl reload // Load last config
# makoctl restore // Load last notify

on-notify=exec canberra-gtk-play -i message-new-instant
